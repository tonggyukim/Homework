using System;
using System.Collections.Generic;

namespace MaximumUnitsOnTruck
{
    class Program
    {
        private static int MaxFinder (IList<(int,int)>boxTypes, int truckSize)
        {
            int maxNumber = 0;

            return maxNumber;
        }
        private int GetInputFromConsole(IList<(int,int)> boxTypes)
        {
            int n = 0;
            for (int i=0;i<boxTypes.; i++)
            return n;
        }
        static void Main(string[] args)
        {
            var boxTypes = new List<(int, int)>
            {
                for ()
            }
            Tuple<int, int> boxTypes = new Tuple<int, int>;
            Console.Write("What kind of box types are there?: ");
            string input = Console.ReadLine();
            string[] inputArray = input.Split(',', ' ');
            int rowCount = int.Parse(inputArray[0]);
            int columnCount = int.Parse(inputArray[1]);
            int[,] array = new int[rowCount, columnCount];
            for (int row = 0; row < r)

            Console.Write("How many boxes can the truck hold?: ");
            string maxBoxes = Console.ReadLine();
            
            
        }
    }
}
